# Vite dev-server proxy target for cross-origin LLM endpoints.
# Set this to your internal gateway URL to bypass CORS during development.
# The proxy intercepts requests prefixed with /llm- and forwards them,
# stripping browser Origin/Referer headers that gateways often reject.
# LLM_PROXY_TARGET=https://your-gateway.example.com

# Separate proxy for Glean Chat API.
# GLEAN_PROXY_TARGET=https://your-tenant.glean.com
